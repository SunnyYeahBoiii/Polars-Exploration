# Query Plan:

In this section there is actually only 2 things:
- We can use `show_graph()` method to visualize the query plan
- We can use `explain()` method to get the query plan as a string
- With the argument `optimized=False` we can visualize or get the explaination for the non-optimized plan
